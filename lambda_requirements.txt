# Lambda-specific requirements
# Core dependencies (from your main requirements.txt)
fastapi==0.115.4
mangum==0.18.0  # ASGI adapter for Lambda
python-dotenv==1.0.0
pydantic==2.9.2
pydantic-settings==2.6.0

# API clients
openai==1.54.3
pinecone-client==5.0.1

# LangChain
langchain==0.3.7
langchain-openai==0.2.8

# Image processing (lightweight)
Pillow==11.0.0

# Utilities
python-multipart==0.0.17

# Note: Some large dependencies might need Lambda Layers or Container Images
# Consider using Lambda Container Images if total size > 250MB unzipped

